apiVersion: apps/v1
kind: Deployment
metadata:
  name: article-api-deployment
  labels:
    app: article-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: article-api
  template:
    metadata:
      labels:
        app: article-api
    spec:
      containers:
        - name: article-api
          image: iad.ocir.io/idaguzmb9zyu/prima/artcile-api:1.0.0-dev
          ports:
            - containerPort: 8080